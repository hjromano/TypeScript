// type annotations

const myName: string = "Higor"
const myAge: number = 30
const isStudent: boolean = true

// observação: essa tipagem pode receber qualquer tipo de valor, o que não é recomendado.

const all: any = "qualquer coisa"

// observação 2