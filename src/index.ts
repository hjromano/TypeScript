function printValue(value: string | number | boolean) {
    if (typeof value === "string") {
        value
        return;
    }
    if (typeof value === "number") {
        value
        return;
    }
    value;
}